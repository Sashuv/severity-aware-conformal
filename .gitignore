# private docs / briefs — keep local, not public
*.md
docs/

# python
__pycache__/
*.pyc
.venv/
venv/

# notebooks
.ipynb_checkpoints/

# secrets / env
.env

# cached claim data
*.jsonl
*.parquet
# but always track small test fixtures (needed for the suite on a clean checkout)
!tests/fixtures/
!tests/fixtures/*.jsonl
*.egg-info/
