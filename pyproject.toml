[project]
name = "sac"
version = "0.1.0"
description = "Severity-aware conformal prediction for medical claim-level hallucination detection"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "scikit-learn>=1.3"]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
